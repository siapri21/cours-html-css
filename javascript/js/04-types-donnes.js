/*----------------TYPES DE DONNEES -----------------*/
// ******


// STRING (chaine de caractère )

const mes = ' je suis un texte';

// NUMBER (tous les types d'écriture numériques sont possibles : positif , négatif , décimal)

const number = 89;
const negatif = -21;
const decimal = 547.83;

// BOOLEAN (booléens)

const boolean = true;
const boolean2 = false;

let text;    //undefinied

const rien =null   //object

// typeof donne le type d'une varaible

console.log(typeof mes)
console.log(typeof number)
console.log(typeof negatif)
console.log(typeof decimal)
console.log(typeof text)
console.log(typeof rien)