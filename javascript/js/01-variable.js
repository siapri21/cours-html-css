/*-------------------------VARIABLES-----------------*/

// declaration d'une variables 

let unevariable;
let UNEVARIABLE;

//affecter une valeur à la variable UNEVARIABLE

/* les variables sont sensible à la case c'est à dire on fait 
LA DIFF ENTRE LES MAJ ET LES MIN*/

unevariable = 'je suis une variable';

// les deux peuvent se faire en meme temps 

let hello = "Hello World"

hello =unevariable;

console.log(hello);

// ------------------------CONSTANTE--------------

const constante = 'je suis une constante';
// LA constante nous permet de déclare une valeur
//  mais on  peut pas une affecte une valeur (on dit qu'elle est immuatable)
// elle garantie que la valeur ne soit pas moidifie tout au long de l'excution du script

EXEMPLE = constante = 'autre chose';

// Le mot VAR permet aussi  de déclarer des variables 
// c'est une pratique désuète (c'était la seule façcon avant 2015)

var past = 'je suis dépassé';
